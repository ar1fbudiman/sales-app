import MainLayout from "../components/MainLayout";

const Registration = () => {
  return (
    <MainLayout
      active="registration"
      title="User Registration"
      desc="Add new User"
    ></MainLayout>
  );
};

export default Registration;
